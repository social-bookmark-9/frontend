import React from "react";
import Navbar from "../components/Navbar";

const Reminder = props => {
  return (
    <React.Fragment>
      <Navbar title={`읽지 않은 아티클 (15)`} />
      <div></div>
    </React.Fragment>
  );
};

export default Reminder;
