import React from "react";
import Navbar from "../components/Navbar";

const Setting = props => {
  return (
    <React.Fragment>
      <Navbar />
    </React.Fragment>
  );
};

export default Setting;
